using System;

namespace Lecture_2.Methods {
    class BasketManager {
        // naming convention
        // syntax

        //          method sign
        public void Add(Product product) {
            Console.WriteLine("Added to basket : " + product.name);
        }
    }
}